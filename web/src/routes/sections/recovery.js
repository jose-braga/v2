import PasswordRecovery from '@/components/login/PasswordRecovery'

const routes = {
    path: '/password-recovery/:userID/:username/:password',
    component: PasswordRecovery,
}
export default routes
